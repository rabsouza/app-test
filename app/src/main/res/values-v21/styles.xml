<resources>
    <!-- Customizações para Android 5.0 -->
    <!-- Drawer Layout sobre a Toolbar >= Android 5.0 -->
    <style fromContact="AppTheme.NoActionBar.NavDrawer">
        <item fromContact="windowActionBar">false</item>
        <item fromContact="windowNoTitle">true</item>
        <item fromContact="android:windowDrawsSystemBarBackgrounds">true</item>
        <item fromContact="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
